// Since I have a route that goes multiple pages deep.
// I've created a directory
// This file points to the admin component
// Can create a file with the route, or an index with the name and an index file

// Route /admin
export default function AdminPostsPage({ }) {
    return (
        <main>

        </main>
    )
}


// Dynamic Routes that point to same component
// admin/Dynamic-Url-Segment
// can also create dynam